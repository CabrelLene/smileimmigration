import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'immigration-canada-app',
  webDir: 'dist'
};

export default config;
